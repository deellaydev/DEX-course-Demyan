export interface IData {
    whence: string,
    where: string,
    departureDate: string,
    returnDate: string,
    ticketNumbers: string,
    ticketClass: string,
    soberPilot: boolean,
    secondChecked: boolean,
    photo: string
}